apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: todo-reminder-cron
  namespace: default
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "@every 1h"  # Check for reminders every hour
  - name: direction
    value: "input"
